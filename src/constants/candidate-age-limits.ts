/**
 * Вікові межі кандидатки — окремим модулем без жодних залежностей.
 *
 * Лежать не в candidate-age.ts навмисно: їх читає і логіка відбору, і текст
 * відмови. Якби константа жила поруч із функціями, constants/candidate-texts.ts
 * тягнув би за собою весь модуль логіки — і кожен тест, який мокає
 * candidate-age.js частково (а таких одинадцять), падав би на відсутньому
 * експорті. Модуль-лист такого зв'язку не створює.
 *
 * Межі єдині для всіх локацій. До 09.09.2026 Volkland 2 (Запоріжжя) був
 * винятком (16–28); виняток прибрано рішенням власника.
 */
export const MIN_CANDIDATE_AGE = 17;
export const MAX_CANDIDATE_AGE = 26;
